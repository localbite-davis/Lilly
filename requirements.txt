# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0        # [standard] pulls in websockets + httptools
python-multipart==0.0.9          # required for request.form() in FastAPI

# ── Telephony ────────────────────────────────────────────────────────────────
twilio==8.10.2

# ── LLM ──────────────────────────────────────────────────────────────────────
anthropic>=0.40.0

# ── Speech (STT + TTS via ElevenLabs) ───────────────────────────────────────
websockets>=12.0                 # ElevenLabs TTS WebSocket stream
httpx>=0.27.0                    # ElevenLabs STT REST API (async)

# ── Memory / Vector store ────────────────────────────────────────────────────
pinecone>=3.0.0                  # Pinecone vector DB + native inference embeddings

# ── Database ─────────────────────────────────────────────────────────────────
SQLAlchemy>=2.0.23
asyncpg>=0.29.0                  # async PostgreSQL driver for NeonDB
psycopg2-binary>=2.9.9           # sync driver for FastAPI legacy routes
aiosqlite>=0.20.0                # async SQLite for local dev without Postgres

# ── Background tasks ─────────────────────────────────────────────────────────
celery==5.3.4
redis==5.0.1

# ── Utilities ────────────────────────────────────────────────────────────────
pydantic>=2.5.2
pydantic-settings>=2.2.0
python-dotenv==1.0.0
httpx>=0.27.0
tenacity>=8.2.0
structlog>=24.0.0

# ── RAG knowledge base (chromadb + scrapers) ────────────────────────────────
chromadb>=0.4.22                # local persistent vector store, default sentence-transformers embeddings
beautifulsoup4>=4.12.0          # MedlinePlus + ACOG HTML scraping
lxml>=5.0.0                     # faster BS4 backend
pymupdf>=1.23.0                 # imported as `fitz` — for optional ACOG PDF ingestion

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
freezegun>=1.4.0
